# features/

Put your own Gherkin feature files here, one directory per project:

```
features/<project>/<Name>.feature
```

Each `.feature` file states business logic in plain language — see the root
README's "The pipeline" section for the format `record` expects. This
directory is empty on purpose: features describe *your* application, so
there is nothing to ship here.
